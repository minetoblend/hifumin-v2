spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  quartz:
    jdbc:
      initialize-schema: never

discord:
  enabled: false
  token: placeholder
